---
title: ".NET Tooling"
format: html
---

# TL;DR
Set up a **.NET 9** development environment for **Pedalogical**. This guide covers installing the .NET SDK, EF Core CLI, and Visual Studio Code with recommended extensions.

## 1. Install .NET SDK 9+

| Platform | Command |
|----------|---------|
| macOS | `brew install --cask dotnet-sdk` |
| Windows | `choco install dotnet-9.0-sdk -y` |

Verify:

```bash
dotnet --version   # 9.x.x
```

## 2. Global EF Core CLI

```bash
dotnet tool install --global dotnet-ef
# add to PATH on macOS
echo 'export PATH="$HOME/.dotnet/tools:$PATH"' >> ~/.zprofile
```

## 3. Visual Studio Code

| macOS | Windows |
|-------|---------|
| `brew install --cask visual-studio-code` | `choco install vscode -y` |

From VS Code **Command Palette** run:

```
Shell Command: Install 'code' command in PATH
```

### 4. Install Recommended Extensions

```bash
code --install-extension ms-dotnettools.csdevkit
code --install-extension ms-dotnettools.csharp
code --install-extension jmrog.vscode-nuget-package-manager
code --install-extension ms-azuretools.vscode-docker
code --install-extension EditorConfig.EditorConfig
```

---

::: {.callout-note}
### Next Step  
Choose your preferred [**Database Setup**](database.qmd) path.
:::
